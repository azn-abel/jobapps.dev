{
  "name": "jobapps.dev",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --workspace=app",
    "preview": "npm run preview --workspace=app",
    "dev:ui": "npm run dev --workspace=app",
    "dev:api": "npm run dev --workspace=api",
    "deploy": "npm run deploy --workspace=api"
  },
  "keywords": [],
  "author": "Abel Lu",
  "license": "ISC",
  "description": "A minimal, no-BS job applications tracker.",
  "workspaces": [
    "app",
    "api",
    "shared"
  ],
  "private": true
}
